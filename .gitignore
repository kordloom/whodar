# Binary
/whodar
/bin/
/dist/
# The cosign signing private key never belongs in the repo; only cosign.pub is
# committed, for verifying releases.
cosign.key
# Compiled site docs generator, built by `go -C site/gen build`.
/site/gen/gen

# Go
*.test
*.out
coverage.*

# Editors
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Local index and ingested work data. Never commit under strict policy.
/data/
/index/
.whodar/
*.bleve

# Cloudflare wrangler local state, written during pages deploy.
.wrangler/
